language: rust
rust: nightly
addons:
  apt:
    packages:
      - libdbus-1-dev
      - libssl-dev
cache:
  directories:
    - /home/travis/.cargo/
install:
  - rustup component add rustfmt
script:
  - cargo test
  - cargo fmt --all -- --check
before_cache:
  - rm -rf /home/travis/.cargo/registry
